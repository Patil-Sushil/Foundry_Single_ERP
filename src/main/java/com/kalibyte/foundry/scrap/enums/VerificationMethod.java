package com.kalibyte.foundry.scrap.enums;

public enum VerificationMethod {
    AUTO_FROM_HEAT,
    VISUAL_INSPECTION,
    SPECTROMETER_TEST,
    ORDER_LINKAGE,
    PHYSICAL_MARKING,
    DEFAULT_MIXED
}
